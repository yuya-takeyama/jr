{
  extends: ['config:base'],
  regexManagers: [
    {
      fileMatch: ['^*.gemspec'],
      matchStrings: [
        ".*add.*dependency '(?<depName>.*?)', '(?<currentValue>.*?)'",
      ],
      datasourceTemplate: 'rubygems',
    },
  ],
}
